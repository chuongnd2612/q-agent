# Q-Agent v2 — handoff

Open `Q-Agent v2.dc.html` in a browser (keep it next to `support.js`).

- `FLOW-CHANGES.md` — what changed vs the previous flow, and notes for re-working the system.
- `Q-Agent v2.dc.html` — the new project-scoped UI.
- `Q-Agent.dc.html` — the previous version, for diffing.
- `PipelineRail.dc.html`, `Q-Agent Auth.dc.html` — supporting components, unchanged.
